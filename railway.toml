[build]
builder = "heroku/buildpacks:22"

[deploy]
startCommand = "python -m uvicorn api.main_hybrid:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"