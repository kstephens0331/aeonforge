{
  "framework": "vite",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skipping root install'",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://backend-production-9820c.up.railway.app/api/$1"
    }
  ]
}