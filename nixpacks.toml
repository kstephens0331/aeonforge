[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['python -m pip install -r api/requirements.txt']

[phases.start]
cmd = 'cd api && python -m uvicorn main_hybrid:app --host 0.0.0.0 --port $PORT'